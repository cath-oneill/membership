<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Club Membership</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><a href="#">Club Membership</a></h1>
        </li>
         <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li class="has-dropdown">
            <a href="#">Logged in as <%= current_user.email %>.</a>
            <ul class="dropdown">
              <li><%= link_to 'Update Password', edit_user_registration_path, :class => '' %></li>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete, :class => ''  %></li>
            </ul>
          </li>
          <li><%= link_to 'Members', members_path, :class => '' %></li>
          <li><%= link_to "Payments", payments_path, :class => ''  %></li>
          <li><%= link_to "Admin", admin_index_path, :class => ''  %></li>
      </section>
    </nav>
    <% end %>

        <% if notice %>
        <div class="row">
          <div class="columns small-12">
            <p class="alert-box success round"><%= notice %></p>
          </div>
        </div>
        <% end %>
        <% if alert %>
        <div class="row">
          <div class="columns small-12">
              <p class="alert-box alert round"><%= alert %></p>
          </div>         
        </div>
        <% end %>

    <div class="row">
      <div class="columns small-12">
        <%= yield %>
      </div>
    </div>
    
    <%= javascript_include_tag "application" %>
  </body>
</html>